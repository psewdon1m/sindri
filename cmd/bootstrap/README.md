# Bootstrap

The repository bootstrap entry point is the root `./sindri` shell launcher.

This directory is reserved for a future compiled bootstrap implementation if the shell launcher becomes too limited.

